import{useEffect,useState}from'react';import{api}from'../lib/api';import type{Alert}from'../types';import{Badge,Card,ErrorBox,Loading,PageTitle}from'../components/UI';
export function AlertsPage(){const[data,setData]=useState<Alert[]>();const[error,setError]=useState('');const load=()=>api<Alert[]>('/alert').then(setData).catch(e=>setError(e.message));useEffect(()=>{void load()},[]);async function resolve(id:number){await api(`/alert/${id}/resolve`,{method:'POST'});load()}return <><PageTitle title="Cảnh báo bất thường" subtitle="Theo dõi và xử lý các sự cố môi trường, thiết bị"/>{error&&<ErrorBox message={error}/>} {!data?<Loading/>:<Card><table><thead><tr><th>Tiêu đề</th><th>Khu vực</th><th>Mức độ</th><th>Trạng thái</th><th>Thời gian</th><th/></tr></thead><tbody>{data.map(a=><tr key={a.alert_id}><td><b>{a.title}</b><small className="table-sub">{a.message}</small></td><td>{a.area_name}</td><td><Badge tone={a.severity==='high'?'red':'yellow'}>{a.severity}</Badge></td><td>{a.status}</td><td>{new Date(a.created_at).toLocaleString('vi-VN')}</td><td>{a.status!=='resolved'&&<button className="secondary" onClick={()=>resolve(a.alert_id)}>Đã xử lý</button>}</td></tr>)}</tbody></table></Card>}</>}
