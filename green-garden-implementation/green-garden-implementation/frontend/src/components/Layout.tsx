import{NavLink,Outlet}from'react-router-dom';import{Activity,AlertTriangle,BarChart3,ClipboardList,Droplets,Gauge,LayoutDashboard,Leaf,LogOut,Settings2,SlidersHorizontal,Users}from'lucide-react';import{useAuth}from'../auth/AuthContext';
const items=[['/','Tổng quan',LayoutDashboard],['/environment','Chỉ số môi trường',Gauge],['/devices','Điều khiển thiết bị',Settings2],['/alerts','Cảnh báo',AlertTriangle],['/history','Lịch sử dữ liệu',BarChart3],['/thresholds','Cấu hình ngưỡng',SlidersHorizontal],['/areas','Khu vực trồng',Leaf],['/tasks','Lịch công việc',ClipboardList],['/users','Người dùng',Users]] as const;
export function Layout(){const{user,logout}=useAuth();return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-icon"><Leaf/></div><div><strong>GREEN GARDEN</strong><small>Hệ thống thủy canh IoT</small></div></div><nav>{items.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'}><Icon size={17}/><span>{label}</span></NavLink>)}</nav><div className="profile"><div className="avatar">{user?.full_name?.[0]||'G'}</div><div><strong>{user?.full_name}</strong><small>{user?.role}</small></div></div><button className="logout" onClick={logout}><LogOut size={17}/>Đăng xuất</button></aside><main><header><div><h2>Hệ thống giám sát vườn thủy canh</h2><p>GREEN GARDEN · cập nhật theo thời gian thực</p></div><div className="header-badge"><Activity size={17}/>Đang hoạt động</div></header><section className="content"><Outlet/></section></main></div>}
