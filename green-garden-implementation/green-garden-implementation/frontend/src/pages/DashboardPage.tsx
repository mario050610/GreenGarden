import{useEffect,useState}from'react';import{AlertTriangle,Droplets,Gauge,Leaf,Lightbulb,Power,Thermometer,Wind}from'lucide-react';import{api}from'../lib/api';import{Card,ErrorBox,Loading,PageTitle}from'../components/UI';
type Dashboard={summary:{areas:number;alerts:number;devices:number;healthySensors:number};latest:Array<{sensor_type:string;unit:string;value?:number;latest?:{value:number}}> ;alerts:Array<{alert_id:number;title:string;message:string;severity:string}>;areaHealth:Array<{area_id:number;area_name:string;crop_type:string;health:number}>;devices:{on:number;off:number;auto:number}};
const icon:Record<string,React.ElementType>={temperature:Thermometer,humidity:Wind,ph:Gauge,ec:Droplets,light:Lightbulb,water_level:Droplets};
export function DashboardPage(){const[data,setData]=useState<Dashboard>();const[error,setError]=useState('');useEffect(()=>{api<Dashboard>('/dashboard').then(setData).catch(e=>setError(e.message))},[]);if(error)return <ErrorBox message={error}/>;if(!data)return <Loading/>;return <><PageTitle title="Tổng quan hệ thống" subtitle="Giám sát toàn diện 6 khu vực thủy canh"/><Card className="hero"><div><small>GREEN GARDEN IOT</small><h2>Tổng quan hệ thống</h2><p>Cập nhật theo thời gian thực từ các cảm biến và thiết bị.</p></div><div className="hero-stats"><span><b>{data.summary.areas}/6</b>Khu vực OK</span><span><b>{data.summary.alerts}</b>Cảnh báo</span><span><b>{data.summary.healthySensors}</b>Cảm biến khỏe</span></div></Card><div className="sensor-grid">{data.latest.slice(0,6).map(s=>{const Icon=icon[s.sensor_type]||Gauge;const value=s.latest?.value??s.value??'-';return <Card key={s.sensor_type}><div className="metric-head"><Icon size={19}/><span>{s.sensor_type}</span></div><div className="metric"><b>{value}</b><small>{s.unit}</small></div><span className="trend">● cập nhật gần đây</span></Card>})}</div><div className="two-col"><Card><h3>Biến động môi trường 24 giờ qua</h3><div className="fake-chart"><svg viewBox="0 0 600 180"><polyline points="0,135 75,126 150,128 225,116 300,103 375,88 450,87 525,101 600,111" fill="none" stroke="currentColor" strokeWidth="4"/><polyline points="0,80 75,76 150,75 225,69 300,59 375,55 450,55 525,64 600,70" fill="none" stroke="#3b82f6" strokeWidth="4"/></svg></div></Card><Card><h3>Cảnh báo gần đây</h3><div className="alert-list">{data.alerts.map(a=><div className={`alert-item ${a.severity}`} key={a.alert_id}><AlertTriangle size={17}/><div><b>{a.title}</b><small>{a.message}</small></div></div>)}</div></Card></div><div className="two-col"><Card><h3>Sức khỏe khu vực trồng</h3>{data.areaHealth.map(a=><div className="health" key={a.area_id}><span>{a.area_name}</span><div><i style={{width:`${a.health}%`}}/></div><b>{a.health}%</b><small>{a.crop_type}</small></div>)}</Card><Card><h3>Tổng quan thiết bị</h3><div className="device-summary"><span><Power/><b>{data.devices.on}</b>Đang bật</span><span><Power/><b>{data.devices.off}</b>Đang tắt</span><span><Leaf/><b>{data.devices.auto}</b>Auto</span></div></Card></div></>}
