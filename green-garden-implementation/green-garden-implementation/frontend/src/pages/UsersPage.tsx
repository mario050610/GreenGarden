import{useEffect,useState}from'react';import{api}from'../lib/api';import type{User}from'../types';import{Badge,Card,ErrorBox,PageTitle}from'../components/UI';
export function UsersPage(){const[data,setData]=useState<User[]>([]);const[error,setError]=useState('');const load=()=>api<User[]>('/user').then(setData).catch(e=>setError(e.message));useEffect(load,[]);async function toggle(id:number){await api(`/user/${id}/toggle`,{method:'POST'});load()}return <><PageTitle title="Quản lý người dùng" subtitle="Tài khoản và phân quyền hệ thống"/>{error?<ErrorBox message={error}/>:<Card><table><thead><tr><th>Họ tên</th><th>Email</th><th>Vai trò</th><th>Trạng thái</th><th/></tr></thead><tbody>{data.map(u=><tr key={u.id??(u as any).user_id}><td><b>{u.full_name}</b></td><td>{u.email}</td><td>{u.role}</td><td><Badge tone={u.status==='active'?'green':'gray'}>{u.status}</Badge></td><td><button className="secondary" onClick={()=>toggle(u.id??(u as any).user_id)}>{u.status==='active'?'Khóa':'Mở khóa'}</button></td></tr>)}</tbody></table></Card>}</>}
